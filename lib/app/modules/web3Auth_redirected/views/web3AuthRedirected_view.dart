import 'package:flutter/material.dart';

class Web3authredirectedView extends StatelessWidget {
  const Web3authredirectedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
